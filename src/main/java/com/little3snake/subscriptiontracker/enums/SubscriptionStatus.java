package com.little3snake.subscriptiontracker.enums;

public enum SubscriptionStatus {
    ACTIVE,
    CANCELED, // canceled by user
    EXPIRED // i don't know - maybe just throw out - add features in the future
}
